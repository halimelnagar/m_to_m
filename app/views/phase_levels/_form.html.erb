<%= form_for(@phase_level) do |f| %>
  <% if @phase_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phase_level.errors.count, "error") %> prohibited this phase_level from being saved:</h2>

      <ul>
      <% @phase_level.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
